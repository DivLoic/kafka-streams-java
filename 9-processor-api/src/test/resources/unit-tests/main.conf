application.id="unittest"
bootstrap.servers="unused:9092"
schema.registry.url="mock://unused:8081"
auto.offset.reset=latest

group.instance.id="local-instance-1"

commit.interval.ms=0
cache.max.bytes.buffering=0

app.topics.input.raw-temperatures="testing-raw-temperature-measures"

app.topics.output.warnings="testing-quality-warnings"
